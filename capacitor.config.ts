import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '10JavaScriptOneLiner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
